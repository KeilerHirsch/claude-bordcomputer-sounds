# Audio clips are (c) Paramount/CBS — never commit them. Fetch via download_sounds.py.
sounds/*.mp3
sounds/*.wav

# Python / misc
__pycache__/
*.pyc
*.log
*.bak
.DS_Store

# --- Secrets / PII (NEVER commit) ---
.env
.env.*
*.pem
*.key
id_rsa*
*.pfx
Local State
Cookies

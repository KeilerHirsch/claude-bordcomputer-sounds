# Dependabot — dependency & GitHub-Actions freshness (repo standard §6; OpenSSF Baseline).
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        update-types:
          - "minor"
          - "patch"
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      pip:
        update-types:
          - "minor"
          - "patch"
